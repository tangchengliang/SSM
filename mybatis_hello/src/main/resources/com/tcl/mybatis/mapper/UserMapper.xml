<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcl.mybatis.mapper.UserMapper">
    <!--int insertUser();
        mapper 接口和映射文件要保证两个一直
        1.mapper接口的全类名和映射文件 namespace一致
        2.mapper接口中的方法和方法名要和映射文件中的sql 的id保持一致
    -->
    <insert id="insertUser">
        insert into t_user values(null ,'admin','123456',23,'男','12345@qq.com')
    </insert>

    <update id="updateUser">
        update t_user set username='roosst', password='123' where id = 1
    </update>

    <delete id="deleteUser">
        delete from t_user where id = 1
    </delete>

    <!--user getUserById();
        resultType: 设置结果类型，即查询的数据要转为的java类型
        resultMap: 自定义映射，处理多对一或一对多
    -->
    <select id="getUserById" resultType="com.tcl.mybatis.pojo.User">
        select * from t_user where id = 3
    </select>
    
    <select id="getAllUser" resultType="User">
        select * from t_user
    </select>
</mapper>